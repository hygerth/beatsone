# beatsone

```
Usage: beatsone [OPTIONS]
Options:
-h, -help       Print this help text and exit
-v, -version    Print program version and exit
-j, -json       Print the result in JSON format
-s, -schedule   Print the schedule
```
